

## 📌 چرا اصلاً logrotate لازم داریم؟  
تامکت معمولاً لاگ‌های زیادی تولید می‌کنه، مخصوصاً وقتی روی یک سرور درحال کار باشه.  
مثلاً فایل‌هایی مثل:

- `catalina.out`
- `catalina.YYYY-MM-DD.log`
- `localhost.YYYY-MM-DD.log`
- `manager.YYYY-MM-DD.log`
- و کلی فایل `.txt` برای access log

اگر اینا رو کنترل نکنیم،  
**حجم دیسک رو پر می‌کنن و سرویس رو به خطر می‌ندازن**.

به همین خاطر ما logrotate رو تنظیم می‌کنیم تا:

- به‌صورت روزانه فایل‌ها رو rotate کنه  
- فقط چند روز آخر رو نگه داره  
- بقیه رو حذف یا gzip کنه  
- حجم کلی کم بشه  
- فایل‌های بزرگ بدون ری‌استارت تامکت مدیریت بشن

---

## 📁 مسیر لاگ‌ها در Tomcat  
در سرور شما پوشه‌ی لاگ‌ها اینجاست:

```
/tomcat/logs/
```

و اینجا پر از فایل‌های روزانه و لحظه‌ای تامکته.

تنها فایلی که به‌صورت پیش‌فرض rotate نمی‌شه،  
**catalina.out**  
هست که اگر رهاش کنیم، تا چند گیگابایت بالا میره.

---

## 🎯 هدف ما  
می‌خوایم کاری کنیم که:

### ✔ فقط ۳ روز آخر نگه داشته بشه  
### ✔ همه فایل‌ها فشرده بشن (gzip)  
### ✔ فایل catalina.out هم آسیب نبینه و درست مدیریت بشه  
### ✔ هیچ‌لاگی از کنترل خارج نشه

---

## 🛠 مرحله ۱: ساخت فایل تنظیمات logrotate  
اول ترمینال رو باز کن و یک فایل جدید بساز:

```
sudo nano /etc/logrotate.d/tomcat
```

---

## 🧩 مرحله ۲: گذاشتن تنظیمات داخل فایل  
این تنظیمات رو کامل داخل فایل بذار:

```conf
/tomcat/logs/*.log
/tomcat/logs/*.txt
/tomcat/logs/catalina.out
{
    daily
    rotate 3
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
```

### توضیحش به زبان ساده:
- **daily** → هر روز یک بار بررسی و rotate کن  
- **rotate 3** → فقط ۳ نسخه قبلی بمونه  
- **compress** → فایل‌های قدیمی gzip بشن  
- **delaycompress** → امروز دست نمی‌زنه، فردا gzip می‌کنه  
- **missingok** → اگر نبود، گیر نمی‌ده  
- **notifempty** → اگر خالی بود، کاری نمی‌کنه  
- **copytruncate** → مخصوص catalina.out که بدون ری‌استارت فایل رو خالی کنه  

---

## 🧪 مرحله ۳: تست کردن تنظیمات  
برای مطمئن شدن از درست بودن کار:

```
sudo logrotate -f /etc/logrotate.d/tomcat
```

اگر درست کار کنه، توی مسیر:

```
/tomcat/logs/
```

لاگ‌ها فشرده می‌شن و فقط ۳ روز قبل باقی می‌مونه.

---

## 🧹 نتیجه‌ای که باید ببینی  
بعد از اجرای درست، وضعیت لاگ‌ها شبیه این می‌شه:

```
catalina.out
catalina.out.1.gz
catalina.out.2.gz
catalina.out.3.gz
catalina.2025-11-15.log.gz
catalina.2025-11-16.log.gz
catalina.2025-11-17.log.gz
localhost_access_log.2025-11-15.txt.gz
...
```

و بقیه حذف می‌شن.

---

## 🎉 تمام!  
